// void, Obj This, point pt

Hero this;

this = This.AsHero();
if (!.IsValid()) return;

//pr ("set_final_party_orientation(" + (pt.x - 1024) + "," + (pt.y - 1024) + ")");

.SetFinalPartyOrientation(pt);

